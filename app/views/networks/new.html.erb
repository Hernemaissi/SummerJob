<%= provide(:title, 'new network') %>
<h1>Create a new network</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@network) do |f| %>

      <%= f.submit "Create a new network", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
