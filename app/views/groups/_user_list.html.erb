<% @users.each do |user| %>
<% if user.group.nil? && !user.isTeacher %>
   <%=link_to user.name,{:controller => "groups", :action => "add_member", :id => @group.id, :user_id => user.id}, :class => 'btn btn-success' %>
<% end %>
<%end%>
