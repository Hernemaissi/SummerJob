
<div class="free_users">
        <%= render "groups/group_users", :users => @free_users %>
     </div>
     <hr class="h1"></hr>
     <div class="taken_users">
       <%= render "groups/group_users", :users => @taken_users %>
   </div>