 <%= f.fields_for :operator_role do |builder| %>
      <p> Service Level </p>
          <div class="size_buttons">
            <%= builder.radio_button :service_level, '1', :checked => true, :class => "level" %>
            <%= label :level_1, 'Budget' %>
          </div>

          <div class="size_buttons">
            <%= builder.radio_button :service_level, '2', :class => "level"%>
            <%= label :level_2, 'Common' %>
          </div>

          <div class="size_buttons">
            <%= builder.radio_button :service_level, '3', :class => "level"%>
            <%= label :level_3, 'Luxury' %>
          </div>
      <div class="clear"></div>
      <div class="size_buttons"
        <%= builder.label :specialized, "Specialized?"  %>
        <%= builder.check_box :specialized,  :class => "specialized" %>
      </div>
      <div class="clear">  <p> Capacity </p></div>
      <div class="size_buttons">
            <%= builder.radio_button :capacity, '1', :checked => true, :class => "capacity" %>
            <%= label :capacity_1, 'Small' %>
          </div>

          <div class="size_buttons">
            <%= builder.radio_button :capacity, '2', :class => "capacity" %>
            <%= label :capacity_2, 'Medium' %>
          </div>

          <div class="size_buttons">
            <%= builder.radio_button :capacity, '3' , :class => "capacity" %>
            <%= label :capacity_3, 'Large' %>
          </div>

       <div class="clear">  <p> Product type </p></div>
      <div class="size_buttons">
            <%= builder.radio_button :product_type, '1', :checked => true, :class => "type" %>
            <%= label :product_type_1, 'Short Flight' %>
          </div>

          <div class="size_buttons">
            <%= builder.radio_button :product_type, '2', :class => "type" %>
            <%= label :product_type_2, 'Medium Flight' %>
          </div>

          <div class="size_buttons">
            <%= builder.radio_button :product_type, '3', :class => "type" %>
            <%= label :product_type_3, 'Long Flight' %>
          </div>
        <%end %>