<%= provide(:title, "#{@company.name}'s results".html_safe)%>
<h1> <%= @company.name %>'s results </h1>
<div id="report">
<p class="left">Current values: </p> <%= link_to "Show detailed reports", company_report_path(@company.id), :class => "btn btn-primary", :id => "reports_button" %>
<div class="clear"></div>
<input type="hidden" name="company_id" id="company_id" value="<%= @company.id %>"/>
<table class="tableizer-table1">

              <%= render 'companies/company_stats_show' %>
              <% if @company.is_customer_facing? %> <%= render 'customer_facing_stats' %> <% end %>
              <tr><td style="background-color:yellow;text-align:left;">Owner:</td>
              <td style="background-color:yellow;text-align:right;">Group <%= @company.group.id %></td>
              </tr>
            </table>
</div>
<p id="ranking"> Ranking: <%= @company.get_ranking %> / <%= @other_companies.size %>  </p>


<p> Statistics </p>

<script type="text/javascript" src="https://www.google.com/jsapi"></script>
    <script type="text/javascript">
      google.load("visualization", "1", {packages:["corechart"]});
      google.setOnLoadCallback(drawChart);
      function drawChart() {
        var data = google.visualization.arrayToDataTable(<%= raw  @company.report_data_table %> );
        data.setColumnProperty(2, 'role', 'tooltip');
        data.setColumnProperty(2, 'html', 'true');
        data.setColumnProperty(4, 'role', 'tooltip');
        data.setColumnProperty(4, 'html', 'true');
        data.setColumnProperty(6, 'role', 'tooltip');
        data.setColumnProperty(6, 'html', 'true');

        var options = {
          title: 'Company Performance',
          hAxis: {title: 'Year', titleTextStyle: {color: 'red'}},
          tooltip: {isHtml: true}
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('profits_chart'));
        chart.draw(data, options);
      }
    </script>
  


<div id="profits_chart" style="width: 900px; height: 500px;"></div>

<script type="text/javascript" src="https://www.google.com/jsapi"></script>
    <script type="text/javascript">
      google.load("visualization", "1", {packages:["corechart"]});
      google.setOnLoadCallback(drawChart);
      function drawChart() {
        var data = google.visualization.arrayToDataTable(<%= raw  @company.launch_data_table %> );

        var options = {
          title: 'Launch Capacity through the game'
        };

        var chart = new google.visualization.LineChart(document.getElementById('capacity_chart'));
        chart.draw(data, options);
      }
    </script>


    <div id="capacity_chart" style="width: 900px; height: 500px;"></div>
   <%= select_tag "variables", options_for_select(@company.get_result_variables, "Launches") %>

    <h2> Notifications </h2>

    <% if @risk %>
    A following accident happened during the launch: <br/>
    <p> <%= @risk.title %> </p>
    <p> <%= @risk.description %> <br/>
    You had to return <%= "#{@risk.customer_return}%" %> of the sales revenue for that launch </p>
    <% else %>
    <p> They year went safely and there were no accidents </p>
    <% end %>
